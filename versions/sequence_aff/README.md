# Sequence AFF

`variant.py` replaces only `hidden1 + hidden2` with mask-conditioned
attentional feature fusion. Complete utterances retain exact branch addition.

The completed 8-rate by 5-seed comparison was below inherited Original on the
seed-macro mean and failed the locked gate.

Results: [`../../results/iemocap6/fold5/sequence_aff`](../../results/iemocap6/fold5/sequence_aff).
