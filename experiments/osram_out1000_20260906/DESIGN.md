# OSRAM capacity variant: H4-Output1000

This is a bounded capacity diagnostic relative to the H4 OSRAM+mean control. Only the OSRAM dimensions (`heads=4, key_dim=32, value_dim=32, output_dim=1000`) changed; the block-delta memory, structured predictor, JEPA loss, masks, cyclic schedule, optimizer, features, and evaluation protocol are unchanged.

This is an internal Test-oracle diagnostic, not a formal paper result.
