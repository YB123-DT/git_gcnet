# PRD：GCNet 第二层聚合器（中文）

实现受控的 GenAgg、带尺度修正的 Soft Medoid、SSMA，以及零新增参数的自定义 RTDR transition-routing 假设；保证 legacy 路径精确不变，继承已有 Original 证据，并让每个接受候选运行相同的6任务正式兼容子集，分两波共24任务。代数冗余或来源机制无法迁移的方案在训练前否决，只上传校验完成的代码与结果。完整主 PRD 见[这里](prd-second-graph-aggregators.md)。
