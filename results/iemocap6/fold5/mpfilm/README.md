# MPFiLM — IEMOCAPSix fixed fold 5

This directory records the completed Linearized FiLM versus Faithful Edge-wise
FiLM diagnostic across 8 missing rates and 5 seeds. The seed-level mean delta
was `+0.000391`; it did not pass the locked stability tests and is recorded as
a negative method result.

The raw NPZ archives remain at the external artifact root in `provenance.json`.
