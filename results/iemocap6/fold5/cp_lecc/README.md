# CP-LECC — IEMOCAPSix fixed fold 5

This directory records the corrected-protocol comparison at requested missing
rates `0.5` and `0.7`, with seeds `66` through `70`. Rates not run under the
corrected protocol are deliberately absent. The method failed its locked
overall promotion rule and remains a negative result.
